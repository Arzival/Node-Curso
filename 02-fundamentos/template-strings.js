const nombre = 'Deadpool';
const real = 'Wade Winston';

console.log(`${nombre} es el nombre real de ${real}`);

const html = `
<h1>Hola mundo</h1>
<p>Mi nombre es ${nombre}</p>
`;

console.log(html);